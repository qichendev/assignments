# Full Stack JavaScript - First Assignment

## Objective

Using Node.js with Express and MongoDB, create a user website that is a CRUD application.

It should manage the following data:

- User Last Name
- First Name
- Date of Birth
- Address1
- Address2
- City
- Postal Code
- Country
- Phone-Number
- Email
- User Notes

The data should be collected by a web form and must use `POST` HTTP methods.

## Requirements

The web application should have 3 pages:

- One to add data
- Another to update data
- A third to display the data in a table

Add a delete button on the update page to delete a user.

- Use PUG or Handlebars to simplify the webpages.
- Use [W3CSS](https://www.w3schools.com/w3css/) or [Bootstrap](https://getbootstrap.com/docs/5.0/getting-started/introduction/) to format the page and add menus and toolbars.
- Use MongoDB Atlas or a local MongoDB instance.

## Reference Material

- [MongoDB Node CRUD Tutorial](https://www.mongodb.com/developer/languages/javascript/node-crud-tutorial/)
- `CRUD-MongoDB-NodeJS`
- [YouTube Tutorial](https://youtu.be/fbYExfeFsI0)
- [Enable TCP Access for All Users](https://www.mongodb.com/docs/atlas/security/ip-access-list/)

**Important:** Adding the CIDR `0.0.0.0/0` allows access from anywhere. Ensure that strong credentials (username and password) are used for all database users when allowing access from anywhere.

## Submission Details

1. Documentation is key, and a lack of adequate comments will reduce your points.
2. Create a development checklist and follow it. Submit this checklist as well.
3. Write a set of instructions on how to use your app.
4. Take screenshots of your completed app for each of the three pages, properly populated, and add them to the ZIP file. If you are using Atlas, please send the login so your app can be verified.

## Instructions

- Please zip your assignment and name it with your `CNumber` before submitting it to this Dropbox. Only ZIP files are accepted, not `.7z`, etc.
- Please add a header comment in your main `index.js` file with your name and `CNumber`.
- The more comments you add, the better your chances of winning the extra points.
- Do not send your `node_modules` directories. They can be recreated from `package.json` and `npm`.
- Please zip your assignment and name it with your `CNumber` before submitting it.

Failing to follow these instructions will lead to points being subtracted.

**Note:** If your files look like they are copied, then all involved will fail.

Good luck!

Terry D'Silva
