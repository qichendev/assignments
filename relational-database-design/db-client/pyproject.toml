[project]
name = "db-client"
version = "0.1.0"
description = ""
authors = [
    {name = "a_litle_tiger",email = "frankfromcn@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "oracledb (>=3.1.1,<4.0.0)",
    "tabulate (>=0.9.0,<0.10.0)"
]

[tool.poetry]
packages = [{include = "db_client", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
